{
  "name": "worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "WRANGLER_LOG_PATH=/tmp/wrangler.log wrangler deploy src/index.ts --dry-run --name panwoo-worker --compatibility-date 2026-05-09",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
